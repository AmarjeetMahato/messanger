import { conversationRelations, conversations } from "./schema/Conversations.model";
import { deviceRelations, devices } from "./schema/Device.model";
import { messageRelations, messages } from "./schema/Messages.model";
import { participantRelations, participants } from "./schema/Participants.model";
import { roleRelations, roles } from "./schema/Role.model";
import { tokenRelations, tokens } from "./schema/Token.model";
import { userRelations, users } from "./schema/User.model";
import { userRoleRelations, userRoles } from "./schema/UsersRole.model";



export const schema = {
    conversations,
     devices,
     messages,
     participants,
     roles,
     tokens,
     users,
     userRoles,
     conversationRelations,
     deviceRelations,
     messageRelations,
     participantRelations,
     roleRelations,
     tokenRelations,
     userRelations,
     userRoleRelations




}